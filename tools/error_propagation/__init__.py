"""Category folder: error_propagation tools. Uncertainty/error propagation through a computation chain. Each module here (except
files starting with '_') must expose TOOL_NAME, TOOL_DESCRIPTION,
open_window (see tools/example/template.py for the pattern)."""
