"""
Category folder: regression tools. Each module in here (except files starting with '_', which are private helpers, e.g. _points.py) must expose TOOL_NAME, TOOL_DESCRIPTION, open_window (see linear.py or tools/example/template.py for the general pattern).
"""