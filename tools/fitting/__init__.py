"""Category folder: fitting tools (regression models -- linear,
polynomial, exponential, logarithmic, custom non-linear, and
multivariate curve fitting). Each module here (except files starting
with '_') must expose TOOL_NAME, TOOL_DESCRIPTION, open_window."""
