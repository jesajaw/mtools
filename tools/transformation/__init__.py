"""Category folder: transformation tools. FFT, frequency filtering & data normalization, Laplace transform. Each module here (except
files starting with '_') must expose TOOL_NAME, TOOL_DESCRIPTION,
open_window (see tools/example/template.py for the pattern)."""
